cd frontend

# Cria arquivo .env.production
@"
VITE_API_URL=https://compartilha-ai-backend.onrender.com
"@ | Out-File -Encoding utf8 .env.production
```

Ou cria manualmente:
```
VITE_API_URL=https://compartilha-ai-backend.onrender.com
```

---

### **1.2 Atualizar o `.gitignore` (não commitar .env local)**

**Arquivo: `.gitignore` (na raiz do projeto)**
```
# Python
__pycache__/
*.pyc
.env
venv/
.venv/
backend/.env

# Node
node_modules/
dist/
.vite/

# Environment (só commita .env.production)
frontend/.env
frontend/.env.local

# Outros
.DS_Store
*.log